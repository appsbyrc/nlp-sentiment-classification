# Small-scale NLP sentiment classification project
This project explores sentiment classification on a small hotel-review dataset using NLP preprocessing, TF-IDF, and several machine learning models. Because the dataset contains only 109 reviews, the results should be treated as a learning-focused case study rather than a production-level benchmark.
